FROM nginx:alpine

# Copy the static assets to the default Nginx html directory
COPY . /usr/share/nginx/html

# Expose port 80
EXPOSE 80

# The default Nginx entrypoint will handle starting the server
