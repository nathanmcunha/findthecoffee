#+TITLE: Project Plan: Coffee Finder (Roaster-Centric Edition)
#+SEQ_TODO: TODO(t) NEXT(n) IN-PROGRESS(i) | DONE(d) CANCELLED(c)

* 🏗️ Phase 1: Infrastructure & Core (Legacy)
** DONE Create multi-stage Dockerfile for the Flask application 🐳
** DONE Configure Docker Compose for local orchestration (App + Postgres)
** DONE Define persistent volumes for PostgreSQL data in Docker
** DONE Implement Database connection class using SQLAlchemy Engine
** DONE Execute initial SQL schema (DDL) using Raw SQL scripts

* 🗄️ Phase 2: Roaster & Inventory Schema (RE-ARCHITECTING)
** DONE Create `roasters` table (id, name, website, location)
** DONE Update `coffee_beans` table: Add `roaster_id` FK and remove `cafe_id`
** DONE Create `cafe_inventory` join table (cafe_id, bean_id)
** DONE Implement `RoasterRepository` with CRUD operations
** DONE Update `CoffeeBeanRepository` to join with `Roasters`
** DONE Update `CafeRepository` to fetch current inventory (Beans + Roaster details)

* 🌐 Phase 3: Discovery API (Finding Roasters & Cafes)
** DONE Implement Roaster List & Detail endpoints
** DONE Implement "Find Cafe by Roaster" search logic
** DONE Enhance Bean Search: Filter by Roaster, Roast Level, and Origin
** DONE Global Discovery Endpoint: Search across Roasters, Beans, and Cafes (FTS implemented)

* 🎨 Phase 4: Frontend & UI (Inventory View)
** DONE Update UI to display Roaster information on Bean cards
** DONE Implement filtering by Roaster on the main discovery page
** TODO Show "Available At" list on Bean detail views (linking back to Cafes)

* ☸️ Phase 5: Kubernetes Orchestration (Legacy)
** DONE Create K8s Deployment and Service manifests for the Flask app
** DONE Configure a StatefulSet for the PostgreSQL instance
** DONE Manage sensitive data (DB passwords) using K8s Secrets
** DONE Implement ConfigMaps for non-sensitive configurations

* 🚀 Phase 6: Polish & Performance
** DONE SQL Performance Audit (FTS Materialized View implemented)
** TODO Document "Roaster -> Bean -> Cafe" domain model in README.md
** TODO Final deployment to cloud provider
